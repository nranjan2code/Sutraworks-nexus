# NEXUS Configuration
# Copy this file to .env and edit as needed

# --- Ollama Teacher Configuration ---
# The host URL where your Ollama instance is running
OLLAMA_HOST=http://localhost:11434

# The model to use for teacher distillation (must be pulled in Ollama)
OLLAMA_MODEL=llama2

# --- Resource Governance ---
# (Optional) Overrides for resource limits if we implement env reading for them later
# ACTIVE_RAM_LIMIT=30.0
# IDLE_RAM_LIMIT=20.0
